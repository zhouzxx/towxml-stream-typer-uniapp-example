<view class="h2w h2w-{{theme}}">
	<view class="h2w__main" wx:if="{{openTyper}}">
		<block wx:for="{{batchIds}}" wx:for-index="i" wx:for-item="item" wx:key="i">
			<batch batchId="{{item}}"/>
		</block>
	</view>
	<view class="h2w__main" wx:else>
		<decode nodes="{{dataNodes}}"/>
	</view>
</view>
